{
    "name": "@jammal/db",
    "version": "1.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "type-check": "tsc --noEmit",
        "migrate": "echo 'Apply migrations via Supabase CLI: npx supabase db push'",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@jammal/shared": "workspace:*"
    },
    "devDependencies": {
        "typescript": "~5.8.3"
    }
}