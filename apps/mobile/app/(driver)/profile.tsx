// ============================================================================
// JAMMAL — Driver Profile (reuses customer profile with driver-specific info)
// ============================================================================

import ProfileScreen from '../(customer)/profile';
export default ProfileScreen;
